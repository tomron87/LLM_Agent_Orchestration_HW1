[pytest]
addopts = -q
testpaths = tests
markers =
    integration: tests that require a running local Ollama server
# Note: unit tests are NOT explicitly marked; we run them via: -m "not integration"

